package com.lhl.${project}.repository;

import com.lhl.${project}.model.${Name};
import com.lhl.fw.api.repository.IBaseRepository;

public interface I${Name}Repository extends IBaseRepository<${Name}, ${Type}> {
 
}